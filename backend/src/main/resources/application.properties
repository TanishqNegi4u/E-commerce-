spring.application.name=shopwave-backend

# Server
server.port=${PORT:8080}
server.servlet.context-path=/api

# ─── Database ─────────────────────────────────────────────────────────────────
spring.datasource.url=jdbc:postgresql://dpg-d6abjlogjchc73f1v2eg-a:tQ35KZZ3KalA0iuFdtpRsMPhVPUD8idg@dpg-d6abjlogjchc73f1v2eg-a.singapore-postgres.render.com:5432/shopwave_db
spring.datasource.username=shopwave_db_user
spring.datasource.password=tQ35KZZ3KalA0iuFdtpRsMPhVPUD8idg
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (Render optimized)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:shopwave-super-secret-jwt-key-change-in-production-2024}
jwt.expiration.ms=${JWT_EXPIRATION_MS:86400000}
jwt.refresh.expiration.ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# Cache
spring.cache.type=simple

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# File Upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.shopwave=INFO
logging.level.org.springframework.security=WARN

# CORS — ADD YOUR FRONTEND DOMAIN
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://skumrender.onrender.com,http://localhost:5500,http://localhost:3000,http://127.0.0.1:5500,http://127.0.0.1:3000,https://e-app-123.netlify.app,null}