# Navigate to your project
cd /path/to/ShopWave

# Check status
git status

# Add all model changes
git add backend/src/main/java/com/shopwave/model/

# Commit
git commit -m "Fix Lombok annotations in model classes"

# Push to GitHub
git push origin main